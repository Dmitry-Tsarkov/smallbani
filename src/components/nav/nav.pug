//- nav

mixin nav(options = {})
  nav.nav
    ul.nav__list
      each item in options.items
        li.nav__item
          a.nav__link(
            href=item.url
            title=item.title
          )=item.title
